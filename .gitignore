node_modules
.env*
!/.env.example
.eve
.vercel
.workflow-data
.next
.output
.nitro
dist
.DS_Store
*.tsbuildinfo

# Convex codegen (regenerated by `npx convex dev`)
convex/_generated/

# Claude Code local state: agent worktrees and machine-local settings.
# (.claude/skills/ IS tracked, so do not ignore all of .claude/.)
.claude/worktrees/
.claude/settings.local.json
